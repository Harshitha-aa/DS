// Online C compiler to run C program online
#include <stdio.h>
void insertion(int a[],int n)
{
    int i,j,key;
    for(i=1;i<n;i++)
    {
        key=a[i];
        for(j=i-1;j>=0;j--)
        {
            if(a[j]>key)
            {
            a[j+1]=a[j];
            }
            else
            {
            break;
            }
        }
        a[j+1]=key;
    }
    for(int k=0;k<n;k++)
    {
        printf("%d",a[k]);
    }
}

int main() {
    int n=6;
int a[n];
for(int i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
insertion(a,n);
    return 0;
}
